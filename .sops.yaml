# TODO: setup different keys for users and hosts
# TODO: more logical configuration
keys:
  - &primary age137a57summs6lwn9yk3tgtln392637xu27wrj277t98ztc7z6s56s6thfck
creation_rules:
  - path_regex: secrets/users/[^/]+/[^/]+\.age$
    key_groups:
      - age:
          - *primary
  - path_regex: secrets/users/[^/]+\.age$
    key_groups:
      - age:
          - *primary
  - path_regex: secrets/users/[^/]+/[^/]+/[^/]+\.age$
    key_groups:
      - age:
          - *primary
  - path_regex: secrets/hosts/[^/]+/[^/]+/[^/]+/[^/]+\.age$
    key_groups:
      - age:
          - *primary
